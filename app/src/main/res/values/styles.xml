<resources>

    <!-- Application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/primary</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
             which is used to tint widgets -->
        <item name="colorAccent">@color/accent</item>
        <!-- You can also set colorControlNormal, colorControlActivated
             colorControlHighlight, and colorSwitchThumbNormal. -->
        <item name="actionBarTheme">@style/ActionBarThemeOverlay</item>
        <item name="actionBarPopupTheme">@style/ActionBarPopupThemeOverlay</item>
        <item name="actionBarSize">@dimen/action_bar_height</item>
        <item name="vpiTitlePageIndicatorStyle">@style/CustomTitlePageIndicator</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
    </style>

    <style name="ActionBarThemeOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--<item name="android:textColorPrimary">#fff</item>-->
        <!--<item name="colorControlHighlight">#3fff</item>-->
    </style>

    <style name="ActionBarPopupThemeOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Indicators styles -->
    <style name="CustomTitlePageIndicator">
        <item name="android:background">@color/primary</item>
        <item name="topPadding">@dimen/page_indicator_top_padding</item>
        <item name="footerPadding">@dimen/page_indicator_footer_padding</item>
        <item name="footerColor">@color/text_white</item>
        <item name="footerLineHeight">@dimen/page_indicator_footer_line_height</item>
        <item name="footerIndicatorHeight">@dimen/page_indicator_footer_indicator_height</item>
        <item name="footerIndicatorStyle">triangle</item>
        <item name="android:textColor">@color/text_secondary_white</item>
        <item name="selectedColor">@color/text_white</item>
        <item name="selectedBold">true</item>
    </style>

</resources>
